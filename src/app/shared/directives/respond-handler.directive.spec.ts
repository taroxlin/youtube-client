import { RespondHandlerDirective } from './respond-handler.directive';

describe('RespondHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new RespondHandlerDirective();
    expect(directive).toBeTruthy();
  });
});

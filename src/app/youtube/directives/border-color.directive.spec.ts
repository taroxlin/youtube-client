import { BorderColorDirective } from './border-color.directive';

describe('BorderColorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderColorDirective();
    expect(directive).toBeTruthy();
  });
});

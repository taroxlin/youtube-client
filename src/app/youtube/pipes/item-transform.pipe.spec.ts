import { ItemTransformPipe } from './item-transform.pipe';

describe('ItemTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
